# Dependencies & build
**/node_modules/
**/.next/
**/out/
**/dist/
**/.turbo/

# Env / secrets (never commit real keys — only web/.env.example with placeholders)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
web/.env
web/.env.local
web/.env.development.local
web/.env.test.local
web/.env.production.local
!.env.example
!web/.env.example

# OS / IDE
.DS_Store
Thumbs.db
*.log
.cursor/
.claude/

# Large local artifacts (optional — remove lines if you want these in git)
*.zip
_prd.zip
_skill.zip
files.zip

# Prisma
**/node_modules/.prisma/
